{
  "_comment": "mcp-server-config.example.json — synthetic placeholder for the gitignored mcp-server-config.json. Copy to mcp-server-config.json and fill in the locally-hosted MCP server's listener config. Gitignored because it can contain auth bindings for sibling MCP processes.",
  "server": {
    "name": "wranngle-n8n-showcase-mcp",
    "version": "0.1.0",
    "transport": "stdio",
    "logLevel": "info"
  },
  "tools": {
    "n8n_workflow_list": {
      "enabled": true,
      "rateLimitPerMinute": 30
    },
    "n8n_workflow_run": {
      "enabled": true,
      "rateLimitPerMinute": 10,
      "allowedWorkflowIds": ["wf_demo_lead_enrichment", "wf_demo_lead_intake"]
    },
    "n8n_credentials_create": {
      "enabled": false,
      "_note": "Credential creation disabled by default — flip to true only on operator machines."
    }
  },
  "auth": {
    "kind": "header",
    "headerName": "X-MCP-Auth",
    "tokenEnvVar": "MCP_SHARED_TOKEN"
  }
}
